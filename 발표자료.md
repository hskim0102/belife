# 아름다운생명사랑 홈페이지 리뉴얼 프로젝트

> 13년 된 레거시 홈페이지(그누보드 기반)를 최신 웹 기술로 재구축한 비영리단체 공식 웹사이트

---

## 1. 프로젝트 개요 (Why)

### 배경
아름다운생명사랑은 **저소득 어르신, 취약계층 어린이, 이주민, 해외 빈민을 위한 의료복지 비영리단체**입니다.
기존 홈페이지는 13년 전 구축된 **그누보드(Gnuboard) 기반 레거시 시스템**으로, 다음과 같은 문제를 안고 있었습니다.

| 문제 | 내용 |
| --- | --- |
| **노후화** | 13년 된 PHP/MySQL 기반, 유지보수 어려움 |
| **모바일 미대응** | PC 전용 레이아웃 → 모바일 사용자 이탈 |
| **관리 난이도** | 비전문가(사무국 직원)가 콘텐츠를 직접 수정하기 어려움 |
| **SEO 취약** | 메뉴가 전부 이미지 → 검색엔진 수집 불가 |
| **자산 소실 위험** | 13년 치 게시글·사진(수천 건)이 특정 서버에 종속 |

### 목표
> **"13년 치 데이터를 완벽히 보존하면서, 비전문가도 쉽게 관리하는 반응형 웹사이트로 재구축한다."**

- ✅ **데이터 보존**: 레거시 게시글·사진 **약 4,000여 건**을 무손실 이관
- ✅ **쉬운 관리**: `/admin` CMS로 코드 없이 콘텐츠 관리
- ✅ **모바일 우선**: 모든 디바이스에서 완벽 동작하는 반응형
- ✅ **검색 최적화**: 시맨틱 HTML + 메타데이터 자동 생성

---

## 2. 사용 기술 (Tech Stack)

### 핵심 스택
| 레이어 | 기술 | 선택 이유 |
| --- | --- | --- |
| **프레임워크** | **Next.js 16** (App Router) | 서버 컴포넌트 기반 최신 풀스택, SSR로 SEO 유리 |
| **언어** | **TypeScript 5** | 타입 안정성, 대규모 유지보수성 |
| **UI 런타임** | **React 19** | 최신 서버/클라이언트 컴포넌트 모델 |
| **스타일링** | **Tailwind CSS 4** | 유틸리티 기반 빠른 반응형 구현 |
| **데이터베이스** | **PostgreSQL** (`pg` 드라이버) | 안정적인 관계형 DB, 배열·GIN 인덱스 등 고급 기능 |
| **에디터** | **Tiptap 3** (WYSIWYG) | 비개발자용 리치 텍스트 편집기 |
| **이미지 스토리지** | **Vercel Blob** | 서버리스 환경에 최적화된 오브젝트 스토리지 |
| **HTML 정제** | **sanitize-html** | 레거시/에디터 HTML의 XSS 방어 |
| **테스트** | **Vitest + Testing Library** | 컴포넌트·유틸 단위 테스트 |

### 기술 선택의 특징
- **ORM 미사용, 순수 SQL + 얇은 쿼리 레이어**
  Prisma 같은 무거운 ORM 대신 `pg` 드라이버 위에 얇은 `query()` / `queryOne()` 헬퍼(`lib/db.ts`)를 두어, 성능과 SQL 제어권을 확보하고 서버리스 콜드스타트를 최소화했습니다.
- **Repository 패턴**
  도메인별 데이터 접근을 `lib/repositories/`(posts, comments, heroSlides, menuPages, notifications 등)로 분리해 관심사를 격리했습니다.
- **Server Actions 중심**
  관리자 기능은 별도 API 서버 없이 Next.js **Server Actions**(`*-actions.ts`)로 구현해 구조를 단순화했습니다.

---

## 3. 인프라 구성 (Infrastructure)

```
┌─────────────────────────────────────────────────────────┐
│                        사용자 (브라우저)                    │
└───────────────────────────┬─────────────────────────────┘
                            │ HTTPS
              ┌─────────────▼─────────────┐
              │      Vercel (호스팅)        │
              │  Next.js 16 SSR/서버컴포넌트  │
              │  Route Handlers / Actions   │
              └──────┬───────────────┬──────┘
                     │               │
        ┌────────────▼───┐   ┌───────▼────────────┐
        │  PostgreSQL     │   │   Vercel Blob       │
        │  (게시글·설정)    │   │  (이미지·첨부파일)    │
        └─────────────────┘   └────────────────────┘
```

### 구성 요소
- **호스팅**: Vercel 서버리스 (Next.js 네이티브 배포)
- **DB 커넥션**: `pg.Pool`을 전역 싱글턴으로 관리(`global.__pgPool`)해 서버리스 함수 재사용 시 커넥션 폭증 방지, `PG_POOL_MAX`로 풀 크기 제어
- **SSL 처리**: `DATABASE_URL`의 `sslmode` 파라미터를 파싱해 pg v9 대응 SSL 옵션을 명시적으로 설정
- **이미지 파이프라인**: 업로드 → 용량(8MB)·형식(JPG/PNG/WEBP/AVIF/GIF) 검증 → Vercel Blob 저장 → 공개 URL 반환
- **환경변수 기반 설정**: `DATABASE_URL`, `BLOB_READ_WRITE_TOKEN`, `ADMIN_PASSWORD`, `SESSION_SECRET` 등으로 인프라와 코드 분리

---

## 4. 데이터베이스 구성 (Database)

### 스키마 관리
- **번호순 마이그레이션 파일**(`migrations/001_init.sql` ~ `013_member_groups.sql`)로 스키마 진화를 버전 관리
- 실행: `node scripts/init-db.mjs` 등 Node 스크립트
- **자동 `updated_at` 트리거**: 행 수정 시 자동 갱신. 단, 게시판은 **조회수만 오를 때는 수정일을 갱신하지 않는** 세심한 트리거(`board_set_updated_at`) 적용

### 주요 테이블
| 테이블 | 역할 |
| --- | --- |
| `posts` | 활동소식·공지 등 콘텐츠 게시글 (태그 배열 + GIN 인덱스) |
| `board_posts` | 일반 게시판 (공지/일반/FAQ, 고정·조회수·발행 상태) |
| `comments` | 댓글 (비밀번호 기반 수정/삭제) |
| `programs` | 사업 소개 (국내/해외/교육) |
| `hero_slides` | 메인 배너 슬라이드 |
| `menu_pages` | CMS 메뉴 페이지 (단체소개·사업소개 하위) |
| `notifications` | 공지 팝업 (노출 빈도 제어) |
| `mission_cards` | 미션/핵심가치 카드 |
| `milestones` | 연혁 (연·월별 타임라인) |
| `members` / `member_groups` | 이사·감사·자문·직원 조직도 |
| `impact_stats` | 성과 지표 (숫자 카운터) |
| `site_settings` | 후원 계좌·연락처·주소·테마색 등 사이트 전역 설정 |

### 설계 포인트
- **CHECK 제약**으로 카테고리·그룹 값 무결성 보장 (예: `category IN ('notice','activity',...)`)
- **목적별 복합 인덱스**로 목록 조회 최적화 (예: `board_posts (published, pinned DESC, created_at DESC)`)
- **PostgreSQL 배열 타입 + GIN 인덱스**로 태그 검색 구현 (`posts.tags TEXT[]`)

---

## 5. 데이터 마이그레이션 (핵심 성과)

> 13년 치 레거시 자산을 **무손실로 이관**한 것이 이 프로젝트의 가장 큰 도전이자 성과입니다.

### 이관 규모
- 활동소식 **약 1,800건**, 사진게시판 **약 2,100건** 등 **총 4,000여 게시글**
- 게시판별 이미지 수천 장 (`belife-activity-images`, `belife-board-images`)

### 이관 프로세스
```
레거시 그누보드 사이트
   │  ① 크롤링 (Python: crawl_belife_*.py)
   ▼
정제된 게시글/이미지 데이터
   │  ② SQL 시드 생성 (seed_*_posts.sql / *.blob.sql)
   ▼
   │  ③ 이미지 → Vercel Blob 이관 (migrate-*-images.mjs)
   │  ④ 본문 HTML sanitize (레거시 cheditor HTML 정제)
   ▼
새 PostgreSQL + Blob 스토리지
```

### 특징
- 그누보드 게시판 코드(`bo_table=dzzz` 등)를 새 카테고리 체계(활동/사진/웹진/동영상/보도/표창/달력 등)로 **매핑**
- 레거시 에디터가 생성한 HTML을 `sanitize-html`로 정제해 **XSS 위험 제거 + 반응형 대응**(고정 width/height 속성 제거)
- 이미지 재압축 스크립트(`recompress-board-blobs.mjs`)로 스토리지 용량 최적화

---

## 6. 편의성 & 관리 기능 (Admin CMS)

비전문가인 사무국 직원이 **개발자 도움 없이** 운영할 수 있도록 `/admin` 대시보드를 구축했습니다.

### 관리 가능 항목
- 📢 **메인 배너**: 히어로 슬라이드 이미지 업로드·교체·삭제
- 📝 **게시판/활동소식**: 글 작성·수정·삭제 (WYSIWYG 에디터)
- 🏢 **단체 소개**: 연혁, 미션 카드, 조직도(멤버·그룹) 편집
- 💼 **사업 소개**: 프로그램 CRUD
- 📄 **메뉴 페이지**: 상단 메뉴 하위 페이지 자유 편집
- 🔔 **공지 팝업**: 노출 여부·빈도 제어
- 🎨 **테마 색상**: 사이트 대표 색상 변경
- ⚙️ **사이트 설정**: 후원 계좌·연락처·주소

### 인증 보안
- **HMAC-SHA256 서명 세션 쿠키**(`lib/auth.ts`): 만료시각을 서명해 위·변조 방지
- **HttpOnly + Secure + SameSite** 쿠키 속성으로 탈취·CSRF 완화
- **타이밍 공격 방어**: `timingSafeEqual`로 비밀번호·서명을 상수 시간 비교
- 세션 유효기간 7일

---

## 7. 사용자 기능 & 특징 (Features)

### 방문자 화면 구성
- **메인 페이지**: 히어로 슬라이드 → 미션 → 사업소개 → 활동소식 → 포토갤러리 → 후원 CTA 섹션
- **단체 소개**: 인사말/연혁(타임라인)/조직도/오시는 길
- **사업 소개**: 국내·해외·교육 사업 목록 및 상세
- **게시판/소식**: 카테고리별 목록·상세, 태그 검색
- **후원 안내**, **개인정보처리방침·이용약관**

### 차별화 특징
| 특징 | 설명 |
| --- | --- |
| **모바일 우선 반응형** | Tailwind breakpoint 기반, 모든 기기 완벽 대응 |
| **댓글 기능** | 비밀번호 기반으로 비회원도 작성·수정·삭제 가능 |
| **플로팅 후원 버튼** | 전 페이지에서 후원 페이지로 빠른 접근 |
| **동적 테마** | 관리자가 사이트 대표 색상을 즉시 변경 |
| **공지 팝업** | 노출 빈도 제어(하루 안 보기 등) |
| **SEO 최적화** | Next.js Metadata API로 페이지별 title·description·OpenGraph 자동 생성, 이미지 메뉴 → 시맨틱 텍스트 전환 |

### 보안 & 품질
- **XSS 방어**: 모든 사용자/레거시 HTML을 `sanitize-html`로 정제 (허용 태그·속성·style 값까지 화이트리스트 검증)
- **`server-only` 격리**: DB·인증·스토리지 모듈이 클라이언트 번들에 유출되지 않도록 강제
- **단위 테스트**: Vitest로 주요 컴포넌트·유틸 검증 (`__tests__/`)

---

## 8. 성과 요약

| 항목 | Before (레거시) | After (신규) |
| --- | --- | --- |
| 기술 스택 | PHP / 그누보드 / MySQL | Next.js 16 / React 19 / PostgreSQL |
| 모바일 | ❌ 미대응 | ✅ 반응형 (모바일 우선) |
| 콘텐츠 관리 | 개발자 필요 | ✅ 사무국 직원 직접 관리 |
| SEO | ❌ 이미지 메뉴 | ✅ 시맨틱 HTML + 메타데이터 자동화 |
| 데이터 | 서버 종속 | ✅ 4,000+ 게시글 무손실 이관 |
| 보안 | 노후 | ✅ HMAC 세션 · XSS 정제 · 상수시간 비교 |

---

## 9. 향후 확장 방향

- 💳 **온라인 결제 후원**: PortOne(카드/카카오페이) 정기·단건 결제 연동
- 📧 **봉사 신청 자동 알림**: 폼 제출 시 담당자 이메일 자동 발송
- 🗺️ **sitemap.xml / robots.txt** 동적 생성 완성
- 📊 관리자 대시보드 통계(방문·후원 현황)

---

*문서 작성일: 2026-07-26 · 프로젝트: belife (아름다운생명사랑 공식 홈페이지)*
