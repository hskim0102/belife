# Lessons Learned — 아름다운생명사랑 홈페이지 리뉴얼 프로젝트

> 13년 된 그누보드 레거시 홈페이지를 Next.js 16 기반으로 전면 재구축한 프로젝트의 회고

**프로젝트 기간**: 2026-05-20 ~ 2026-08-04 (약 2.5개월, 37 커밋)
**산출물**: TypeScript 소스 133개 파일 / 약 11,000 LOC, 마이그레이션 SQL 36개, 이관 데이터 4,000여 게시글 · 이미지 4,181장
**작성일**: 2026-08-19

---

## 1. 잘한 점 (What Went Well)

### 1-1. 현업 실무자의 명확한 요구조건 — 김은미 님

이 프로젝트가 2.5개월이라는 짧은 기간에 완주할 수 있었던 **가장 큰 동력은 현업(김은미 님)의 요구조건이 처음부터 구체적이고 명확했다는 점**입니다.

| 기여                               | 내용                                                                                                  | 프로젝트에 미친 영향                                                                                           |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| **문제의 정확한 진단**             | "노후화·모바일 미대응·비전문가 관리 불가·SEO 취약·자산 소실 위험" 5가지를 실무 관점에서 명확히 짚어줌 | 개발 목표가 기술 나열이 아닌 **해결해야 할 문제 목록**으로 정의됨                                              |
| **운영자 관점의 기능 정의**        | "사무국 직원이 개발자 없이 배너를 바꾸고, 연혁을 고치고, 글을 쓸 수 있어야 한다"                      | Admin CMS의 범위(배너·연혁·미션카드·조직도·프로그램·메뉴페이지·팝업·테마·사이트설정)가 흔들림 없이 확정        |
| **데이터 보존 요건의 명시**        | "13년 치 게시글·사진·자료실 파일이 **유지되어야 함**" — 타협 불가 조건으로 선언                       | 무손실 이관이 '나중에 할 일'이 아닌 **1순위 설계 제약**이 되어, 크롤링→시드→Blob 이관 파이프라인을 초기에 구축 |
| **레거시 구조에 대한 도메인 지식** | 구 사이트에 대한 모든 지식을 잘 전달함.                                                               | 개발자가 추측으로 매핑할 수 없는 부분이 **정확한 카테고리 체계**로 즉시 확정                                   |
| **일관된 피드백 주기**             | 주기적으로 구체적 개선 요청                                                                           | 요구사항이 한꺼번에 쏟아지지 않고 **소화 가능한 단위로 분할 도착**, 재작업 리스크 최소화                       |

**핵심 교훈**
현업의 요구조건이 명확하면 개발자의 추측 비용이 사라집니다. 이 프로젝트에서 "이게 맞나요?"를 되묻느라 낭비된 사이클이 거의 없었던 것은, 김은미 님이 **"무엇을 왜 원하는지"를 실무 언어로 정리해준 덕분**입니다. 특히 데이터 보존을 협상 대상이 아닌 전제 조건으로 못 박아준 것이 프로젝트 아키텍처 전체의 기준점이 되었습니다.

### 1-2. 꼼꼼한 테스트 진행 — 윤동현 님

**개발자가 스스로 발견하기 어려운 결함을 실사용자 관점에서 집요하게 찾아준 것**이 품질을 결정적으로 끌어올렸습니다.

| 기여                            | 내용                                                                                                                   | 프로젝트에 미친 영향                                                                                                  |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| **실사용 시나리오 기반 검증**   | 개발 환경의 샘플 데이터가 아닌, **이관된 4,000건 실데이터**를 대상으로 목록·상세·페이징·태그 검색을 전수에 가깝게 확인 | 레거시 cheditor가 만든 깨진 HTML, 고정 width로 모바일에서 넘치는 이미지 등 **데이터에서만 드러나는 버그**를 조기 검출 |
| **멀티 디바이스 교차 검증**     | 모바일·태블릿·PC, 브라우저별로 동일 화면을 반복 확인                                                                   | 반응형 레이아웃 깨짐이 배포 후가 아닌 **개발 중에** 수정됨                                                            |
| **관리자 기능의 실사용 테스트** | 실제 운영자처럼 글 작성·수정·삭제, 이미지 업로드, 배너 교체, 테마 변경을 반복 수행                                     | 에디터 이미지 업로드, 첨부파일, 히어로 슬라이드 등 **Admin 동선의 예외 케이스**가 다수 보완                           |
| **재현 가능한 리포트**          | "어느 화면에서 / 어떤 조건으로 / 무엇이 잘못됐는지"를 명확히 전달                                                      | 원인 추적 시간이 짧아져 **수정 리드타임 단축**                                                                        |
| **회귀 확인**                   | 수정본 배포 후 이전 결함이 재발하지 않는지 재확인                                                                      | 같은 버그가 두 번 보고되는 일이 사실상 없었음                                                                         |

**핵심 교훈**
자동화 테스트(Vitest)는 "내가 예상한 것"만 검증합니다. 윤동현 님의 수동 테스트는 **"내가 예상하지 못한 것"**을 담당했고, 이 조합이 실제 품질을 만들었습니다. 특히 다양한 디바이스로 여러가지 테스트를 진한 한 검증은 어떤 단위 테스트로도 대체할 수 없었던 영역입니다.

### 1-3. 기술적으로 잘된 판단

- **과감한 아키텍처 전환 (Sanity CMS → 자체 PostgreSQL + Admin)**
  초기 계획은 Sanity.io 헤드리스 CMS였으나, "4,000건 레거시 데이터의 완전한 소유·통제"와 "외부 SaaS 종속 없는 운영"이라는 요건과 맞지 않는다고 판단해 2026-06-07(`95999af`)에 **16,524줄 추가 / 20,722줄 삭제**의 전면 전환을 단행했습니다. 4개월 뒤가 아니라 **3주차에 결정**했기에 매몰 비용이 최소화되었습니다.
- **ORM 미사용 결정**
  초기 계획의 Prisma 대신 `pg` 드라이버 + 얇은 `query()/queryOne()` 헬퍼(`lib/db.ts`)를 택해, 서버리스 콜드스타트를 줄이고 SQL 제어권을 확보했습니다.
- **Repository 패턴 분리**
  `lib/repositories/` 하위로 도메인별 데이터 접근을 격리해, 기능이 계속 추가되는 상황에서도 구조가 무너지지 않았습니다.
- **보안 기본기 준수**
  HMAC-SHA256 서명 세션 + `timingSafeEqual` 상수시간 비교, `sanitize-html` 화이트리스트 기반 XSS 방어, `server-only`로 DB/인증 모듈의 클라이언트 번들 유출 차단.
- **번호순 마이그레이션 체계**
  `001_init.sql` ~ `017_hero_slide_text.sql` + 시드 스크립트로 스키마 진화를 추적 가능하게 유지했습니다.

---

## 2. 아쉬웠던 점 (What Could Be Improved)

### 2-1. 초기 기술 스택 선정이 요건 검증보다 앞섰다

README의 최초 지시는 **MySQL + Prisma + Zustand + Sanity**였으나, 최종 산출물은 **PostgreSQL + 순수 SQL + 자체 Admin**입니다. 스택을 먼저 확정하고 요건을 맞춘 순서였기 때문에 3주차에 대규모 재작업이 발생했습니다.

> **개선안**: 킥오프 시 "데이터 소유권 / 운영 주체 / 외부 서비스 종속 허용 범위"를 먼저 확정하고, 그 결과로 스택을 도출한다. Phase 0에 **스파이크(기술 검증) 1주**를 배치한다.

### 2-2. 커밋 위생(Commit Hygiene)이 후반으로 갈수록 무너졌다

초기에는 `feat: add Footer component`처럼 원자적이고 컨벤션을 지킨 커밋이었으나, 후반에는 `커밋`, `커커밋`, `20260803 추가요청 사항 수정`(45파일 2,168줄) 같은 **날짜 단위 배치 커밋**으로 퇴화했습니다.

| 시점        | 커밋 스타일                | 평균 변경 규모 |
| ----------- | -------------------------- | -------------- |
| 5/20 (초기) | `feat:` / `fix:` 원자 커밋 | 1~2 파일       |
| 6월 이후    | 날짜 기반 묶음 커밋        | 20~100 파일    |

> **개선안**: 어떤 요청이 어떤 코드 변경으로 이어졌는지 추적할 수 없으면 회귀 원인 분석이 불가능해진다. 최소한 **요청 항목 단위**로 커밋을 쪼개고, 커밋 메시지에 요청 출처(예: `feat(admin): 뉴스 카테고리 관리 — 7/25 요청 #3`)를 남긴다.

### 2-3. 테스트 코드와 마이그레이션 SQL이 버전 관리에서 제외됐다

`.gitignore`에 `__tests__/`, `migrations/`, `docs/`가 등재되어 있어, **로컬 테스트 파일 20개 중 8개만 git에 추적**되고 있습니다. 마이그레이션 SQL 36개와 설계 문서 전체도 저장소에 없습니다.

이는 다음을 의미합니다.

- 다른 개발자가 클론해도 **DB를 재현할 수 없다**
- CI에서 테스트를 돌릴 수 없다
- 스키마 변경 이력이 로컬 디스크에만 존재해 **유실 위험**

> **개선안**: 즉시 `.gitignore`에서 `__tests__/`, `migrations/`, `docs/` 를 제거하고 전량 커밋한다. 빌드 산출물(`tsconfig.tsbuildinfo`)은 반대로 ignore에 추가한다. 이 프로젝트의 최우선 후속 조치.

### 2-4. 자동화 테스트가 수동 테스트를 뒷받침하지 못했다

윤동현 님이 찾아준 결함들이 대부분 **회귀 테스트로 고착화되지 않았습니다**. 테스트는 컴포넌트 렌더링과 유틸 함수 위주(20개)에 머물렀고, 다음 영역은 비어 있습니다.

- Server Actions (관리자 CRUD 전체)
- Repository 계층의 SQL 쿼리
- 인증·세션 검증 로직
- 이미지 업로드 파이프라인의 검증 규칙(용량·형식)

> **개선안**: 수동 테스트에서 발견된 버그는 **수정과 동시에 재현 테스트를 추가**하는 것을 규칙으로 삼는다. 특히 Server Actions와 auth는 보안 영역이므로 자동 검증이 필수다.

### 2-5. 대용량 바이너리와 저장소 관리

`Images/1.jpg`(8MB), `Images/2.jpg`(7MB) 등 원본 이미지가 git에 커밋되어 있고, 작업 디렉터리에는 506MB 백업 zip이 방치되어 있습니다. 저장소가 무거워지면 클론·CI 시간이 직접 악화됩니다.

> **개선안**: 원본 자산은 Blob/오브젝트 스토리지에 두고, 저장소에는 최적화된 결과물만 둔다. 백업 아카이브는 작업 디렉터리 밖으로 분리한다.

### 2-6. 미완료 범위

초기 요건 중 다음이 미구현 상태입니다.

- **PortOne 온라인 결제 후원** (정기/단건 카드·카카오페이)
- **봉사 신청 이메일 자동 발송** (Resend/Nodemailer)
- **sitemap.xml / robots.txt 동적 생성**

> **개선안**: 미완료 항목을 회고 시점에 **명시적으로 백로그화**하고 우선순위·담당·목표 시점을 지정한다. "향후 확장"이라는 표현으로 뭉뚱그리면 영영 착수되지 않는다.

---

## 3. 핵심 교훈 (Key Takeaways)

1. **명확한 요구조건은 최고의 생산성 도구다.**
   김은미 님의 사례처럼, 현업이 "무엇을 왜 원하는지"를 실무 언어로 정리해주면 개발자의 추측·재작업 비용이 0에 수렴한다. 반대로 요구조건이 모호하면 아무리 좋은 스택도 재작업을 막지 못한다.

2. **자동화 테스트와 사람의 테스트는 대체재가 아니라 보완재다.**
   윤동현 님의 실데이터·실기기 검증은 단위 테스트가 원리적으로 잡을 수 없는 영역을 담당했다. 다만 그 결과를 자동 테스트로 되먹임하지 않으면 지식이 축적되지 않는다.

3. **잘못된 선택은 빨리 버리는 것이 이익이다.**
   3주차의 Sanity → PostgreSQL 전환은 당시엔 뼈아팠지만, 2개월 뒤였다면 프로젝트가 좌초했을 것이다. **매몰 비용보다 남은 기간이 더 비싸다.**

4. **작동하는 코드보다 재현 가능한 코드가 중요하다.**
   마이그레이션 SQL과 테스트가 버전 관리 밖에 있으면, 지금 잘 도는 사이트도 **다시 만들 수 없는 사이트**다.

5. **레거시 이관은 기술 문제가 아니라 도메인 지식 문제다.**
   그누보드 게시판 코드를 새 카테고리로 매핑하는 작업의 난이도는 SQL이 아니라 "이 게시판이 실제로 무엇이었는가"를 아는 데 있었다. 현업의 참여 없이는 불가능했다.

---

## 4. 다음 프로젝트 액션 아이템

| #   | 액션                                                                       | 우선순위 | 근거                   |
| --- | -------------------------------------------------------------------------- | -------- | ---------------------- |
| 1   | `.gitignore`에서 `__tests__/`·`migrations/`·`docs/` 제외 해제 후 전량 커밋 | 🔴 즉시  | 2-3 · 재현 불가 리스크 |
| 2   | Server Actions·auth·repository 대상 테스트 추가                            | 🔴 높음  | 2-4 · 보안 영역 무검증 |
| 3   | 수동 테스트 발견 버그 → 회귀 테스트 동시 작성 규칙화                       | 🟡 높음  | 1-2 · 지식 축적        |
| 4   | 커밋 단위를 요청 항목 단위로 복원, 메시지에 요청 출처 명시                 | 🟡 중간  | 2-2 · 추적성           |
| 5   | 킥오프에 Phase 0(요건 검증 스파이크 1주) 배치                              | 🟡 중간  | 2-1 · 스택 재작업 방지 |
| 6   | 미완료 3건(결제·이메일·sitemap) 백로그 등재 및 일정 확정                   | 🟢 중간  | 2-6                    |
| 7   | 대용량 바이너리 저장소 분리, 빌드 산출물 ignore 처리                       | 🟢 낮음  | 2-5                    |

---

## 5. 감사의 말

이 프로젝트의 결과물은 개발만으로 만들어지지 않았습니다.

- **김은미 님** — 13년 치 자산과 사무국 운영 현실을 가장 잘 아는 실무자로서, 흔들림 없는 요구조건과 정확한 도메인 지식을 제공해 주셨습니다. 데이터 보존을 타협 불가 조건으로 못 박아 주신 판단이 이 프로젝트 아키텍처의 기준점이 되었습니다.
- **윤동현 님** — 개발자가 볼 수 없는 각도에서 집요하게 검증해 주셨습니다. 실데이터 전수 확인과 멀티 디바이스 교차 검증이 없었다면, 지금 발견된 결함들은 오픈 이후 사용자가 마주했을 것입니다.

두 분의 기여는 "잘된 협업의 결과"가 아니라 **"이 프로젝트가 성립한 조건"**이었습니다.

---

_문서 작성일: 2026-08-19 · 프로젝트: belife (아름다운생명사랑 공식 홈페이지)_
